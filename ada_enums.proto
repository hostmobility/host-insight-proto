syntax = "proto3";
package ada;

enum StatusCode {
  OK = 0;
  WARNING = 1;
  ERROR = 2;
}

enum UnitControlStatus {
  UNIT_READY = 0;
  AWAITING_CONTROL = 1;
  UNIT_UNAVAILABLE = 2;
}

enum GpioState {
  INACTIVE = 0;
  ACTIVE = 1;
}